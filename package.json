{
  "name": "MCEG",
  "version": "1.0.0",
  "private": true,
  "description": "Movement Control Event Generator REST api + Next.js frontend",
  "scripts": {
    "develop:backend": "yarn --cwd backend dev",
    "develop:frontend": "wait-on http://localhost:8000 && yarn --cwd frontend dev",
    "dev": "npm-run-all -l -p develop:*"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "wait-on": "5.2.1"
  }
}
