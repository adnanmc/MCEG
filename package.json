{
  "name": "my-site",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "develop:backend": "yarn --cwd backend dev",
    "develop:frontend": "wait-on http://localhost:8000 && yarn --cwd frontend dev",
    "develop": "npm-run-all -l -p develop:*"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "wait-on": "5.2.1"
  }
}
